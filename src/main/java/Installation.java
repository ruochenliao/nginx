/**
 * 安装
 * 1、brew install nginx
 * 2、按照对应的提示网上找对应的漏洞
 *
 * config
 * 查找路径命令: nginx -t
 * 一般会在 /usr/local/etc/nginx/nginx.conf
 *
 * 启动
 * sudo nginx
 *
 * 重新加载 config
 * nginx -s reload
 *
 */
public class Installation {
}
